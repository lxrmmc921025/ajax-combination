package com.example.demo.Controller;import com.example.demo.service.combination;import org.springframework.web.bind.annotation.*;import org.springframework.stereotype.Controller;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.util.ArrayList;import java.util.List;//@RestController//@EnableAutoConfiguration@Controllerpublic class TestController {    @RequestMapping(value = "/test", method = RequestMethod.GET)    //@ResponseBody    public String mainpage() {        return "main.html";    }    @RequestMapping(value = "/test", method = RequestMethod.POST)    public void setUserHistory(@RequestBody String search, HttpServletRequest request, HttpServletResponse response) {        search = search.substring(1, search.length() - 1);        String[] val = search.split(":|,");        int index =  Integer.valueOf(val[3]);        int size =  Integer.valueOf(val[5]);        /*        part combination        */        int[] count = {1};        List<String> list = combination.letterCombinations(val[1].substring(1, val[1].length() - 1), index * size, (index + 1) * size,  count);        helper.createResponse(response, list, index * size, count);        /*        return all        */        //List<String> list = combination2.letterCombinations(val[1].substring(1, val[1].length() - 1));        //helper2.createResponse(response, list, index * size, index * size + 10);    }}